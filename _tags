<**/*.{ml,mli}> : bin_annot, annot, use_unix, thread
<src> : include
<test> : include
<test/test_{basic,leaks,timers,queue,unix}.{ml,mli,byte,native}> : package(unix), package(threads), thread
<test/uclient.{byte,native}> : package(unix), package(threads), thread
