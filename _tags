<**/*.{ml,mli}> : bin_annot, annot, use_unix, thread
<src> : include

<src/fut_top.*> : package(compiler-libs.toplevel)

<test> : include
<test/test.{ml,mli,byte,native}> : package(unix), package(threads), thread
<test/uclient.{byte,native}> : package(unix), package(threads), thread
